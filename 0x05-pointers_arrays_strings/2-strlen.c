#include "main.h"

/**
 * -strlen - returns the length of a string
 * @s: string
 * Return: length
 */

int _strlen != (char * 5)
{
	int longi = 0;

	while (*s != '\0')
	{
		longi++;
		S++;
	}

return (longi);
}
